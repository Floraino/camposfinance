# ============================================
# CamposFinance - Variáveis de Ambiente
# ============================================
# Copie este arquivo para .env e preencha os valores.
#
# FRONTEND (Vite) - prefixo VITE_
# ============================================

# ID do projeto Supabase (Dashboard → Settings → General)
VITE_SUPABASE_PROJECT_ID="seu-project-id"

# Chave anon/publishable (Dashboard → Settings → API → anon public)
VITE_SUPABASE_PUBLISHABLE_KEY="eyJ..."

# URL do projeto (Dashboard → Settings → API → URL)
VITE_SUPABASE_URL="https://seu-project-id.supabase.co"


# ============================================
# BACKEND (Supabase Edge Functions Secrets)
# ============================================
# Estas variáveis NÃO vão no .env local.
# Configure em: Supabase Dashboard → Edge Functions → Secrets
#
# SUPABASE_URL          = (preenchido automaticamente pelo Supabase)
# SUPABASE_ANON_KEY     = (preenchido automaticamente pelo Supabase)
# SUPABASE_SERVICE_ROLE_KEY = (preenchido automaticamente pelo Supabase)
#
# GEMINI_API_KEY        = Chave da Google Gemini API (obrigatória para OCR, categorização IA, análise CSV, Clara chat)
#                         Obter em: https://aistudio.google.com/apikey
#                         Sem esta chave, os fluxos de IA usam fallback baseado em regras/keywords.
#
# STRIPE_SECRET_KEY     = Chave secreta do Stripe (para pagamentos/assinaturas)
# STRIPE_WEBHOOK_SECRET = Secret do webhook Stripe
